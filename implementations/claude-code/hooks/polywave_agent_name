#!/usr/bin/env bash
# Polywave Agent Naming Helper (E44)
# Generates standardized [polywave:{phase}:{identifier}] names for agent launches
#
# Usage:
#   polywave_agent_name wave 1 A logging-injection "inject logger"
#   polywave_agent_name scout - - user-auth "analyze codebase"
#   polywave_agent_name critic - - logging-injection "review briefs"
#
# Output: [polywave:wave1:agent-A] inject logger

set -euo pipefail

phase="$1"
wave_or_dash="${2:--}"
agent_or_dash="${3:--}"
slug="$4"
description="${5:-}"

case "$phase" in
  wave)
    if [[ "$wave_or_dash" == "-" || "$agent_or_dash" == "-" ]]; then
      echo "Error: wave phase requires wave number and agent ID" >&2
      exit 1
    fi
    echo "[polywave:wave${wave_or_dash}:agent-${agent_or_dash}] ${description}"
    ;;
  scout)
    echo "[polywave:scout:${slug}] ${description}"
    ;;
  critic)
    echo "[polywave:critic:${slug}] ${description}"
    ;;
  scaffold)
    echo "[polywave:scaffold:${slug}] ${description}"
    ;;
  integration)
    if [[ "$wave_or_dash" == "-" ]]; then
      echo "Error: integration phase requires wave number" >&2
      exit 1
    fi
    echo "[polywave:integration:wave${wave_or_dash}] ${description}"
    ;;
  *)
    echo "Error: unknown phase '$phase'. Valid: wave, scout, critic, scaffold, integration" >&2
    exit 1
    ;;
esac
