completion_date: "2026-03-10"
feature_slug: yaml-structured-sections-v3
lint_command: go vet ./...
quality_gates:
    gates:
        - command: cd /Users/dayna.blackwell/code/scout-and-wave-go && go build ./...
          required: true
          type: build
        - command: cd /Users/dayna.blackwell/code/scout-and-wave-go && go test ./pkg/protocol/...
          required: true
          type: test
        - command: cd /Users/dayna.blackwell/code/scout-and-wave-go && go vet ./...
          required: false
          type: lint
        - command: cd /Users/dayna.blackwell/code/scout-and-wave-web && go build ./...
          required: true
          type: build
        - command: cd /Users/dayna.blackwell/code/scout-and-wave-web && go test ./pkg/api/...
          required: true
          type: test
        - command: cd /Users/dayna.blackwell/code/scout-and-wave-web/web && command npm test
          required: true
          type: test
    level: standard
state: SCOUT_PENDING
suitability_assessment: |
    **Verdict:** SUITABLE

    This work is suitable for SAW parallelization. It removes the Stub Report from scope (shell script complexity blocker identified in audit v2), leaving 3 Scout-generated sections with clean parser integration points. Hard cutover approach eliminates backward compatibility complexity. File ownership is disjoint across 3 repos with clear boundaries.

    **Scope reduction from v2:**
    - Stub Report migration REMOVED (shell script + orchestrator coordination complexity)
    - Agent count reduced from 10 to 8 (removed agents C for scan-stubs.sh changes)
    - Focus on Scout-generated sections only: Quality Gates, Post-Merge Checklist, Known Issues

    **Estimated times:**
    - Scout phase: ~8 min (dependency mapping, interface contracts, IMPL doc)
    - Wave 1 execution: ~20 min (4 agents in parallel: protocol schemas, Scout prompt, Go types, parser changes)
    - Wave 2 execution: ~15 min (2 agents in parallel: IMPLManifest field integration, API routes)
    - Wave 3 execution: ~12 min (3 agents in parallel: UI panel updates for TypeScript YAML parsing)
    - Merge & verification: ~5 min per wave (15 min total)
    - Total (SAW): ~70 min

    **Sequential baseline:** ~120 min (8 agents × 15 min avg sequential time)
    **Time savings:** ~50 min (42% faster)

    **Recommendation:** Proceed. Hard cutover simplifies implementation and testing. Stub Report remains in markdown format (acceptable — it's orchestrator-generated, not Scout-generated).
test_command: cd /Users/dayna.blackwell/code/scout-and-wave-go && go test ./pkg/protocol/... && cd /Users/dayna.blackwell/code/scout-and-wave-web && go test ./pkg/api/... && cd web && command npm test
title: Migrate Quality Gates, Post-Merge Checklist, and Known Issues to YAML Structured Format
verdict: SUITABLE
